# Copie para .env.local (desenvolvimento) ou coloque na Vercel (produção).
# Sem estas duas linhas o app funciona normalmente, só que guardando os dados
# apenas no aparelho de quem está usando.

VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...

# Opcional: e-mail da conta única da Loreta (o segredo é a senha).
# VITE_LORETA_EMAIL=caixa@loreta.app
